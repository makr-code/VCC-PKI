# VCC-PKI Environment Configuration
# ==================================
# Copy this file to .env and configure with your actual values
# NEVER commit .env file to version control!

# ============================================================================
# CA Password Configuration (REQUIRED)
# ============================================================================
# This password protects the Intermediate CA private key
# SECURITY REQUIREMENTS:
# - Minimum 16 characters
# - Mix of uppercase, lowercase, numbers, and special characters
# - Use a secure password generator
# - Store in a secure secret management system in production
#   (Azure Key Vault, AWS Secrets Manager, HashiCorp Vault)
VCC_INTERMEDIATE_CA_PASSWORD=

# ============================================================================
# Database Configuration (Optional - defaults to SQLite)
# ============================================================================
# For PostgreSQL in production:
# DB_TYPE=postgresql
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=vcc_pki
# DB_USER=pki_admin
# DB_PASSWORD=

# ============================================================================
# Notification Configuration (Optional)
# ============================================================================
# Email notifications for certificate expiry and errors
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=pki@example.com
# SMTP_PASSWORD=
# SMTP_FROM=pki@example.com
# ALERT_RECIPIENTS=admin@example.com

# Slack notifications
# SLACK_WEBHOOK_URL=
# SLACK_CHANNEL=#pki-alerts

# ============================================================================
# Security Configuration
# ============================================================================
# Enable mTLS for service-to-service authentication (recommended for production)
# MTLS_ENABLED=true

# API rate limiting
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_PER_MINUTE=60

# ============================================================================
# HSM Configuration (Optional - for hardware security modules)
# ============================================================================
# HSM_ENABLED=false
# HSM_TYPE=softhsm  # or pkcs11, cloudhsm
# HSM_PIN=
# HSM_SLOT=0
# HSM_LIBRARY_PATH=/usr/lib/softhsm/libsofthsm2.so

# ============================================================================
# Development/Testing (DO NOT USE IN PRODUCTION)
# ============================================================================
# LOG_LEVEL=DEBUG
# RELOAD_ON_CHANGE=true

# ============================================================================
# SECURITY BEST PRACTICES
# ============================================================================
# 1. Never commit this file with actual secrets to version control
# 2. Use different passwords for development, staging, and production
# 3. Rotate passwords regularly (every 90 days recommended)
# 4. Use a password manager or secret management system
# 5. Restrict file permissions: chmod 600 .env
# 6. In production, use environment variables from secure vault
# 7. Enable audit logging and monitor for suspicious activity
# 8. Keep private keys encrypted and in secure storage
# 9. Use mTLS for all service-to-service communication
# 10. Regularly review and update dependencies for security patches
